:javascript
  sortable_tree_init({
    id: "#{opts[:klass]}_nested_set_#{opts[:rand_id]}",
    max_levels: #{opts[:max_levels]},
    rebuild_url: '#{polymorphic_url(opts[:namespace] + [node], action: :disable)}'
  });
